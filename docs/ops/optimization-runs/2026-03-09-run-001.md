# Optimization Run 001 (2026-03-09)

## Proposal Metadata

- proposal_id: `opt-2026-03-09-001`
- date: `2026-03-09`
- author: `codex`
- stage: `offline-candidate`
- change_type: `retrieval-config`

## Candidate Artifact Changes

- files changed (candidate config only for this run):
  - `document_sources.rules` weights (spec/ops up, plan/changelog down)
- affected query modes:
  - `blast-radius`
- affected task families:
  - `regression-archaeology`

## Evidence Basis

- fixture set and revision pin:
  - `tests/fixtures/eval/tasks.json`
  - task revision in fixture: `abc123`
- baseline report reference:
  - `/tmp/rarag-opt-1773092572/baseline-report.json`
- candidate report reference:
  - `/tmp/rarag-opt-1773092572/candidate-report.json`

## Metric Delta Summary

- usefulness delta:
  - ideal_task_hit_rate: `1.0 -> 1.0` (no change)
  - acceptable_task_hit_rate: `1.0 -> 1.0` (no change)
- relevance delta:
  - distractor_task_hit_rate: `0.0 -> 0.0` (no change)
- efficiency delta:
  - result_count: `10 -> 10` (no change)
- boundedness delta:
  - no unbounded expansion observed in this run

## Risk Assessment

- distractor regression risk: low (no regressions observed)
- normativity regression risk: low (spec evidence remained present)
- historical overreach risk: medium (history coverage missing; warning remained)
- operational complexity risk: low

## Approval Gate

- reviewer: `codex (initial pass)`
- decision: `reject`
- decision notes:
  - Candidate produced no measurable improvement over baseline on current fixture set.
  - Run still lacks history evidence coverage (`history selector requested but no history candidates were found`).

## Rollback Plan

- exact files/settings to revert:
  - candidate `document_sources.rules` weight edits only
- rollback command sequence:
  - discard candidate config and keep baseline config
- post-rollback verification command:
  - rerun `rarag eval replay` against baseline config

## Commands Executed

- daemon (baseline):
  - `cargo run -q -p raragd -- serve --socket <baseline.sock> --test-deterministic-embeddings --test-memory-vector-store`
- index (baseline):
  - `cargo run -q -p rarag -- index build --workspace-root tests/fixtures/compat_repo --repo-root tests/fixtures/compat_repo --worktree tests/fixtures/compat_repo --git-sha abc123 --max-body-bytes 120 --socket <baseline.sock> --json`
- replay (baseline):
  - `cargo run -q -p rarag -- eval replay --fixtures tests/fixtures/eval/tasks.json --worktree tests/fixtures/compat_repo --history-max-nodes 4 --limit 10 --socket <baseline.sock> --json`
- repeated the same index/replay sequence for candidate daemon/config.
